Help on: map

Use "map large" for a larger view of the map.
Also see "map legend" for explanation of 
characters.

Some areas (like outerworld) have multiple layers.
You can move up and down on those areas in some locations.
Each layer has its own map.

